# Compiled binaries (at root level only)
/logstream
/producer
/consumer
/logstreamctl
/broker

# Claude Code context file (local development only)
CLAUDE.md

# Test logs
test-logs/

# Data/log directories (runtime generated)
data/
data_producer_test/
logs/

# Binaries
*.exe

# Vagrant local state (contains private keys and machine-specific data)
.vagrant/
